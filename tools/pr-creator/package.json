{
  "name": "pr-creator",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p ./",
    "clean": "rimraf ./dist",
    "lint": "eslint src --ext ts --max-warnings 0"
  },
  "dependencies": {
    "azure-devops-node-api": "~14.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "eslint": "^9.12.0",
    "rimraf": "^6.0.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3",
    "commander": "^12.1.0"
  }
}
